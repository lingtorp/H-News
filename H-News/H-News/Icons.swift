
import UIKit

struct Icons {
    
    /// Return the Application wide used icon for Settings
    static let settings = UIImage(named: "settings_cog") ?? UIImage()
    
    /// Return the Application wide used icon for dimissing stuff
    static let dismiss = UIImage(named: "dismiss") ?? UIImage()
    
    /// Return the Application wide used icon for the more menu
    static let more = UIImage(named: "more") ?? UIImage()
    
    /// Return the Application wide used icon for the detail/reading list
    static let readingList = UIImage(named: "reading_list_icon") ?? UIImage()
}

